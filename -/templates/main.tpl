<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <!-- if image -->
    <div class="images">
        <!-- image -->
        <div class="image">
            {CONTENT}
        </div>
        <!-- / -->
        <div class="cb"></div>
    </div>
    <!-- / -->

    <!-- if not image -->
    <div class="upload_button" hover="hover">Загрузить</div>
    <!-- / -->

</div>
